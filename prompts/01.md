<!-- Role Prompt -->

Você é um desenvolvedor full stack sênior, especializado em Next.js.

<!-- Contexto -->

Tecnologias utilizadas:

- Next.js
- Prisma
- shadcn/ui

- SEMPRE use shadcn como biblioteca de componentes
- SEMPRE use componentes que estão em @app/\_components/ui/page.tsx
- NUNCA use cores hard-coded do Tailwind, APENAS cores do tema que estão em @app/globals.css..
- Use a página que está em @app/page.tsx como referência para criar e organizar o código.
- **SEMPRE** use o MCP do Context7 para buscar documentações, sites e APIs

<!-- Tarefa -->

Crie a página que está em https://www.figma.com/design/KBlNBjp5XXWUj64ZCiT9lq/Aparatus?node-id=10-6869&m=dev usando Figma MCP se possível.

Seja 100% fiel ao Figma **CUSTE O QUE CUSTAR**.

Pegue os dados do banco de dados usando o ID que é recebido como parâmetro na rota.

O botão de "Reservar" NÃO DEVE fazer nada.

O botão de "Copiar" telefone deve copiar o telefone para o clipboard do usuário.

A imagem do banner da página no topo deve ser a imagem da barbearia no banco de dados.

O botão de voltar no topo da página deve voltar a página inicial do projeto.

A imagem de cada serviço deve ser o campo "imageUrl" da tabela "BarbershopService".

Crie a página em @app/barbershops/[id]/page.tsx.

<!-- Plano de implementação -->

<!-- 1. Estrutura da Página (app/barbershops/[id]/page.tsx) -->

- Criar componente async que recebe id como parâmetro
- Buscar dados da Barbearia + serviços usando Prisma ( include services)
- Implementar notFound() se a barbearia não existir

<!-- 2. Hero Section -->

- Imagem do banner usando barbershop.imageUrl do banco
- Altura de 297px conforme Figma
- Botão de voltar (ChevronLeft) que redireciona para "/"
- Fundo branco arredondado sobrepondo a imagem

<!-- 3. Informações da Barbearia -->

- Avatar circular 30px com imagem da barbearia
- Nome da barbearia (Plus Jakarta Sans Bold, 20px)
- Endereço (muted-foreground, 14px)
- Divider usando componente horizontal

<!-- 4. Seção "Sobre nós" -->

- Título "SOBRE NÓS" (uppercase, bold, 12px)
- Descrição da barbearia do banco de dados
- Divider

<!-- 5. Seção "Serviços" -->

- Título "SERVIÇOS" (uppercase, bold, 12px)
- Mapear todos os serviços do banco em cards:
  - Imagem do serviço (110x110px, rounded-[10px])
  - Nome e descrição so serviço
  - Preço formatado (converter de cents para reais)
  - Botão "Reservar" (que não faz nada)
- Usar Card do shadcn com fundo --card-background

<!-- 6. Seção "Contato" -->

- Título "CONTATO" (uppercase, bold, 12px)
- Mapear array de telefones (barbershop.phones)
- Ícone de smartphone + número
- Botão "Copiar" que copia o telefone para clipboard (client component necessário)

<!-- 7. Footer -->

- Component Footer existente ou inline
- Fundo secondary, copyright text

<!-- 8. Componente cliente para copiar telefone -->

- Criar pequeno componente client "use client" para o botão de copiar
- Usar navigator.clipboard.writeText()

<!-- 9. StyLing -->

- Seguir exatamente as classes Tailwind do Figma
- Usar APENAS cores do tema (var(--primary), var(--foreground), etc.)
- Font: Plus Jakarta Sans (já configurada)
- Manter dimensões e espaçamentos do Figma